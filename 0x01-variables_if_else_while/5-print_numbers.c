#include <stdio.h>

/**
 * main - Entry point
 * Return: Always zero for success
 * Description: prints all single number of base 10
 */
int main(void)
{
	int x;

	for (x = 0; x  <= 9; x++)
{
		printf("%d", x);
}
	printf("\n");

	return (0);
}
