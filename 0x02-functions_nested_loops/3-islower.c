#include "main.h"
/**
 * _islower - Checks for lower case character
 * Return: 1 for lowecase or 0 for anything
 * @c: Character to be printed
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
{
		return (1);
}
		return (0);
}
